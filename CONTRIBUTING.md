# 贡献与开发规范

## 1. 开始前

任何实现任务开始前必须阅读：

1. [基线与决策治理](./docs/BASELINE_AND_DECISIONS.md)
2. 与任务相关的功能规格
3. 对应的架构、上下文、Provider、数据、UI 或测试文档
4. [Cherry Studio 双文档参考指南](./docs/CHERRY_STUDIO_REFERENCE_GUIDE.md)及其映射的相关 Cherry 开发者/用户文档
5. 涉及模型、参数、端点或工具时，读取对应官方最新文档章节；官方 wire 事实优先于 Cherry 的 Provider 说明

然后检查工作区状态、现有改动和相关测试。不要覆盖用户未提交的文件，不做与任务无关的重构。

## 2. 工作方式

每个任务先写清：

```text
目标
假设
不做什么
涉及文件/模块
数据或接口变化
验收标准
验证命令
```

如果需求有多种合理解释且会显著改变结果，应先提出问题。对于可以从现有文档和代码确定的小细节，按已有模式保守实现。

## 3. 最小纵向切片

优先完成可以端到端验证的最小功能：

```text
领域契约
-> 数据/协议 codec
  -> 应用用例
  -> UI
  -> 自动化测试
  -> 文档状态
```

不要为了未来可能需要而提前增加抽象、配置、后台任务或模块。单次使用的逻辑先保持直接，只有真实重复和复杂度出现后再抽象。

## 4. 改动范围

- 每一行改动应能追踪到当前任务。
- 不顺手整理无关格式、注释、命名和死代码。
- 自己的改动造成未使用 import/变量时应清理。
- 发现无关问题可以在最终报告中指出，不直接扩大修复范围。
- Cherry `docs` 是任务相关时必须查阅的外部工程/产品参考；其他参考项目仍只读且按需读取。参考源码不得复制，读取后必须记录采用的行为契约、差异和许可边界。

## 5. 基线变更

不得用实现现状反向改写当前规范。需要改变已接受决策时：

1. 说明现有决定和实际问题。
2. 给出至少一个较小修复和一个替代方案。
3. 比较收益、成本、风险、迁移和验证。
4. 获得用户明确确认。
5. 更新受影响规范或建立被接受的决策记录。
6. 再修改实现。

## 6. TypeScript 规则

- 开启严格类型检查。
- 外部 JSON、Provider 事件、数据库 JSON 和导入数据必须运行时校验。
- 避免 `any`，边界处无法避免时尽快收窄到 `unknown` + validator。
- 领域逻辑使用纯函数或明确服务，避免埋在 React effect 中。
- 不从组件直接执行散落 SQL或拼 Provider Body。
- 高频 stream reducer 保持确定性。
- 不使用模型 ID 白名单作为最终能力判断。
- 深合并和点路径实现必须防原型污染。

## 7. React 与状态

- history、streaming 和 UI state 分离。
- 组件订阅最小 selector。
- 历史消息使用稳定 key 和 memo。
- effect 必须清理 listener、timer、observer 和 object URL。
- 列表、按钮和工具区使用稳定尺寸，避免流式布局跳动。
- 所有交互完成键盘和无障碍状态。
- 使用现有 shadcn/Radix/lucide 体系，不手绘已有图标。

## 8. Rust 边界

- Rust 只实现通用网络/流式管道和 Tauri 注册。
- 不添加 Provider 名称、模型判断、参数合并、上下文选择或消息解析。
- 所有 running request 在成功、失败、取消和 Channel 关闭时清理。
- 改动 Rust 后运行 fmt、clippy 和 test。

## 9. 协议 profile 与 codec 规则

每个 profile/codec 变更必须回答：

- 最终 URL、显式端口、protocol profile 和 revision 是什么。
- 参数从哪里合并，未知字段是否保留。
- 历史工具调用/结果如何序列化。
- thinking、usage、finish reason 和 response id 如何解析。
- reasoning/tool 意图如何映射到当前 endpoint 的精确 wire path。
- 临时错误如何结构化分类，是否允许进入 RetryPolicy。
- anchor 失效如何处理。
- 哪些 fixture 覆盖成功、错误、分片和取消。

禁止只以“真实 API 测试能回答”作为唯一验证，因为它无法稳定检测历史线协议缺失。

自动重试变更还必须证明：同一 logical request 的 attempts 使用相同 ContextManifest/body hash；已有语义输出后不从头重发；已完成客户端工具不重复执行；手动重新生成与自动 attempt 不混用。

## 10. 数据库与迁移

- schema 变更只增加新迁移，不修改已发布迁移。
- migration 和关键多表写入使用事务。
- SQL 参数化。
- 更新导入导出版本、fixture 和数据文档。
- 大型工具结果和附件使用 artifact 引用。

## 11. 测试要求

- 修 bug 时先添加可复现测试，或至少在同一改动中加入。
- 工具结果、参数、上下文和协议兼容问题必须在适当边界捕获真实输出。
- 不只测试 happy path。
- 重试、退避和倒计时使用 fake clock/可控随机源，不用真实长时间 sleep。
- 不更新大范围 snapshot 来掩盖行为变化。
- 性能相关改动必须证明 ContextManifest 未改变。
- 测试数据不使用真实密钥、私人对话或用户文件。

## 12. 当前安全范围

产品安全、隐私、密钥存储和发布安全当前为 `deferred`，不作为功能实现或 MVP 验收门禁。开发协作仍不得把真实密钥、私人数据或凭据提交到仓库、示例、测试 fixture 和任务报告；这属于工作区卫生，不代表已经完成产品安全设计。

## 13. 文档同步

实现变化后检查：

- 根 README 的当前状态。
- 对应功能规格状态和验收。
- 架构/数据/协议文档。
- 测试策略和开发路线。
- Cherry 双文档参考指南中的映射是否仍覆盖本次改动；若 Cherry 在线 `main` 与本地快照不同，记录核对日期和差异。
- 官方 API 兼容矩阵中的来源、核对日期和 preset revision；Claude、OpenAI API、ChatGPT、Grok、Gemini 均从文档中心列出的官方入口开始核对。
- 用户可见行为和错误文案。

规划功能不能写成已完成。测试未运行必须明确报告。

## 14. 开源与第三方参考

- Eternal Chat 计划开源，但项目许可证由项目所有者在发布前决定；贡献者不得擅自加入或宣称使用某一许可证。
- Cherry Studio 用户文档和开发者 `docs` 是长期参考生命线，但目录仍只读。借鉴设计时记录来源和抽象思想，所有实现从零完成。
- 不复制参考项目源码、测试、样式、SVG、图标、品牌、文案或素材，即使技术上可以直接粘贴。
- 发布前必须加入 `LICENSE`、第三方依赖许可/NOTICE 清单，并完成源码与素材来源审阅。
- 根 README、About 页面和发行材料必须保留 [NBSearch 鸣谢](./ACKNOWLEDGEMENTS.md)。
- 若未来确需复用第三方代码，必须先确定其精确许可证、版本、版权声明、兼容性和归属要求，并获得用户明确批准；当前规范不授权此行为。

## 15. Git 工作流

项目本地 `main` 仓库已经初始化并关联 [Timmyzzo/Eternal-Chat](https://github.com/Timmyzzo/Eternal-Chat)。截至 2026-07-27 远端为空；首次提交和推送仍需项目所有者明确要求：

- 提交前检查状态、diff、未跟踪文件、删除和敏感信息。
- 一个提交保持单一目的。
- 不提交构建产物、真实数据库、日志、trace、API Key 或本地用户数据。
- 未经用户明确要求，不自动 push、强制 push、改写远端历史或发布。
- 不使用破坏性 reset/checkout 覆盖用户改动。

## 16. 完成报告

最终报告应包含：

- 完成的行为，而非只列文件。
- 关键设计决定和与当前规范的关系。
- 实际运行的验证命令和结果。
- 未运行的测试及原因。
- 已知风险、未完成内容和下一阶段前置条件。

不要把计划中的后续功能包装为当前成果。
